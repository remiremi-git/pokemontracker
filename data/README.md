# Published Runtime Data

`data/published` was the original local-only output folder. The Vercel-ready default is now `public/pokemon-tabletop`.

Generated files:

- `manifest.json`
- `moves.json`
- `abilities.json`

TODO: Set `PUBLISHED_DATA_BASE_URL` to the public static-hosting base URL before publishing.

Optional: set `PUBLISH_UPLOAD_DIR` to copy generated JSON into a local static-hosting folder.
